const ARBITRAGE_TYPE = require('./arbitrage-type');
const EXCHANGE_NAME = require('./exchange-name');
const FUNDING_TYPE = require('./funding-type');
const REGEX = require('./regex');

module.exports = { ARBITRAGE_TYPE, EXCHANGE_NAME, FUNDING_TYPE, REGEX };
